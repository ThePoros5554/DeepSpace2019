package frc.robot.commands.autoScripts;

import edu.wpi.first.wpilibj.command.CommandGroup;
import frc.robot.Robot;
import frc.robot.commands.drive.FollowPath;
import poroslib.util.Path;

public class TestPath extends CommandGroup
{
    public TestPath()
    {
        // Path lP = new Path();
        // addSequential(new FollowPath(Robot.drivetrain, leftPath, rightLeft, distanceKP, distanceKD, headingKP, headingKD))
    }
}